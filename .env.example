DATABASE_URL=postgres://postgres:password@localhost:5433/assetwatch_db?sslmode=disable
JWT_SECRET=dev-secret-change-me
ALLOWED_EMAIL_DOMAIN=sbs.com.pg
PORT=4083

# Optional. Leave SMTP_HOST empty to skip actually sending email.
SMTP_HOST=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM=
